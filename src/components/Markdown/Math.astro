---
import katex from 'katex';
import 'katex/dist/katex.css';

type Props = {
    exp: string;
};

const { exp } = Astro.props;

const formatted = katex.renderToString(exp);
---

<div
    class="mb-6 text-center"
    set:html={formatted}
/>
