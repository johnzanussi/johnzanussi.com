---
import type { MarkdownHeading } from 'astro';

export type Props = {
    headings: MarkdownHeading[];
    url: string;
};

import TableOfContents from '@/components/TableOfContents/TableOfContents.astro';

const { headings } = Astro.props;
const hasHeadings = !!headings.length;
---
<aside class="content-sidebar w-100 xl:mx-auto xl:sticky xl:top-12 xl:max-h-[calc(100vh-3rem)] xl:overflow-auto sm:grow xl:self-start sm:mt-5 mb-3 xxl:mb-0 pb-2 xl:pb-12 xl:ps-12">

    {hasHeadings && <TableOfContents headings={headings} />}

</aside>
