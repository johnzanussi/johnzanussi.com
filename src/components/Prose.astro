---
type Props = {
    class?: string;
    tag?: string;
};

const {
    class: className = '',
    tag: Element = 'div',
    ...props
} = Astro.props;
---

<Element
    class:list={['prose dark:prose-invert', className]}
    {...props}
>
    <slot />
</Element>
