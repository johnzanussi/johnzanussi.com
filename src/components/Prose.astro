---
type Props = {
    class?: string;
    tag?: string;
    full?: boolean;
};

const {
    class: className = '',
    tag: Element = 'div',
    full = false,
    ...props
} = Astro.props;
---

<Element
    class:list={['prose', className]}
    {...props}
>
    <slot />
</Element>
