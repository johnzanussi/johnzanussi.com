---
import type { HTMLAttributes } from 'astro/types';

export type Props = HTMLAttributes<'span'>;

const { class: className = '' } = Astro.props;
---
<span class:list={[
    'p-1 rounded-1 bg-warning text-dark small text-uppercase fw-bold',
    className
]}>
    Draft
</span>
