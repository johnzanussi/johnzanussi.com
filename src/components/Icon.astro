---
// https://iconify.design/
import { Icon } from 'astro-iconify';

const { class: className, icon } = Astro.props;

const classes = [
    className,
    'icon',
];
---

<Icon
    name={icon}
    class:list={classes}
/>
