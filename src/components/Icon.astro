---
export interface Props {
    icon: string;
    class?: string;
    label?: string;
}

// https://iconify.design/
import { Icon } from 'astro-iconify';

const { class: className, icon, label } = Astro.props;

const classes = [
    className,
    'icon',
];
---

<Icon
    name={icon}
    class:list={classes}
    aria-label={label || `${icon.split(':').pop()} icon`}
/>
