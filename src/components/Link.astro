---
const { href, ...props } = Astro.props;

if (href?.startsWith('http')) {
    props.target = '_blank';
    props.rel = 'noopener noreferrer';
}
---

<a
    href={href}
    {...props}
>

    <slot />

</a>
